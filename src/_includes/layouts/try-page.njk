{% extends "layouts/base.njk" %}

{% block beforeContent %}
  {{ govukBreadcrumbs({
    classes: "govuk-!-display-none-print",
    items: breadcrumbItems
  }) if showBreadcrumbs }}
{% endblock %}

{% block content %}
  {# Note: H1 heading is rendered by JavaScript in try-page.ts based on auth state #}
  {# - Unauthenticated: "Sign in to view your try sessions" #}
  {# - Authenticated: "Your try sessions" #}
  {{ content | safe }}
{% endblock %}

{# Note: try.bundle.js is loaded globally via eleventy.config.js scripts array #}
{# This ensures try.bundle.js is available on all pages for OAuth callback handling #}
